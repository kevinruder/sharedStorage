/**
 * Created by Kevin on 12/01/2016.
 */
Template.markers.onCreated(function(){
    //this.subscribe("storage");

});

Template.markers.helpers({

/*
    newFunc: function(){
        var Result =  RegisterStorage.find({"Space":22}).fetch();

        var latlng = [];

        var k;

        for(k in Result){
            console.log(Result[k].Address);
            result = Result[k].Address;
            var lat = Meteor.call("getLat", result);
            var long = Meteor.call("getLong", result);


            }

        return Result;
    }
*/


});
